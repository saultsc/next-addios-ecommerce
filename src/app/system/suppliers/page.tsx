import { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Proveedores',
	description: 'Maneja tus proveedores',
};

export default function SuppliersPage() {
	return <div className=""></div>;
}
