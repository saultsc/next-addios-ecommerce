import { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Productos',
	description: 'Maneja tus productos',
};

export default function ProductsPage() {
	return <div className=""></div>;
}
