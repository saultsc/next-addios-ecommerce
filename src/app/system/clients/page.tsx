import { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Clientes',
	description: 'Maneja tus clientes',
};

export default function ClientsPage() {
	return <div className=""></div>;
}
